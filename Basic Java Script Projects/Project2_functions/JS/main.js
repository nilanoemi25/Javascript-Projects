function MyFunction() {   //function
    var B = 5;  // expression
    var A = ++B; 
    document.getElementById("pizza").innerHTML = A; // method on document property 
  }

  function MyCon(){
      var soti = "I like eating";   // String in use
      soti += " pasta and pizza!"; // using operator to concatenate 
      document.getElementById("concatenate").innerHTML = soti; // using innerHTML to change the HTML 
  }

